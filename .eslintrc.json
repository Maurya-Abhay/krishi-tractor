{
  "extends": ["next/core-web-vitals"],
  "rules": {
    // plugin not always available in some CI/dev environments; disable for now
    "@typescript-eslint/no-explicit-any": "off",
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "prefer-const": "error"
  }
}
